{
  "name": "@rabblerouser/group-mail-receiver",
  "version": "0.0.1",
  "description": "A lambda function that receives mail intended for group from SES and puts corresponding events onto the kinesis stream",
  "scripts": {
    "simulate": "node simulate.js",
    "test": "NODE_ENV=test mocha -r src/__tests__/testGlobals.js --recursive src/__tests__"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rabblerouser/group-mail-receiver.git"
  },
  "license": "AGPL-3.0",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "sinon": "^4.3.0",
    "sinon-chai": "^2.14.0"
  }
}
